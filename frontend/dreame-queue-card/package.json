{
  "name": "ha-dreame-queue-card",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend package for the HA Dreame queue card",
  "scripts": {
    "build": "vite build",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "verify:packaged": "node scripts/verify-packaged-card.mjs",
    "check": "npm run lint && npm run test && npm run build && npm run verify:packaged"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "happy-dom": "^20.9.0",
    "typescript": "^5.8.2",
    "vite": "^7.3.3",
    "vitest": "^3.0.8"
  },
  "dependencies": {
    "lit": "^3.3.3"
  }
}
